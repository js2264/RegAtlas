---
title: "%TITLE"
date: "%DATE"
author: Jacques SERIZAY
output:
    html_document:
        theme: flatly
        highlight: tango
        preserve_yaml: true
        code_folding: show
        df_print: tibble
        toc: true
        toc_float: true
---

# %TITLE

IMPORTANT: This code has been written by Jacques SERIZAY, for the purpose of analysing datasets generated in the Ahringer lab, at the Gurdon Institute, University of Cambridge.  

---  

## Set up chunks options 
```{r "chunk-options_%SUFFIX"}
knitr::opts_chunk$set(echo = TRUE, fig.path = '_figures-files/', warning = FALSE, message = FALSE, tidy = 'styler', fig.align = 'center', cache = TRUE, cache.path = '.cache/')
```

---  

## Set up environment
```{r "env-setup_%SUFFIX", collapse=T}
    if (!isTRUE(getOption('knitr.in.progress'))) {PROJECT_PATH <- '~/%FOLDER/'} else {PROJECT_PATH <- '~/tissueSpeWormBook/'}
    setwd(PROJECT_PATH)
    require(tidyverse)
    require(magrittr)
    require(rtracklayer)
    dir.create('_figures-files/', showWarnings = F)
```

---  

## Load variables 
```{r "var-setup_%SUFFIX"}
    load('~/shared/data/classification_tissue-spe-genes-REs.RData')
    # -----------
    
```

---  

## Define functions 
```{r "fun-setup_%SUFFIX"}
    
    # -----------
    .sourceDirs()
```

---  

## Perform analysis
### Part 1
```{r "chunk-1_%SUFFIX"}
    
```

---  

## SessionInfo 
```{r "sessionInfo_%SUFFIX", echo=FALSE}
    sessionInfo()
```
